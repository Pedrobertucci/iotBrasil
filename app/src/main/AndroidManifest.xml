<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="bertucci.pedro.iotbrasil">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".login.MainLogin">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".configuracoes.MainConfiguracoes" />
        <activity android:name=".Devices.MainDevices" />
        <activity android:name=".Devices.MainPerfilDevice" />
        <activity android:name=".getway.MainGetway" />
        <activity android:name=".getway.ProfileGetway"></activity>
    </application>

</manifest>